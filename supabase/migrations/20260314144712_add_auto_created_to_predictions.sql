/*
  # Add auto_created column to predictions table

  1. Changes
    - Add auto_created column (boolean, default false)
    - This tracks whether a prediction was auto-generated by the system
      or manually created by an admin

  2. Purpose
    - Enables automatic prediction drafting workflow
    - Allows filtering between manual and automated predictions
*/

ALTER TABLE predictions 
ADD COLUMN IF NOT EXISTS auto_created BOOLEAN DEFAULT FALSE;